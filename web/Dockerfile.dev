FROM node:22-slim

WORKDIR /app

# 의존성 먼저 설치 (레이어 캐시: package.json 변경 시에만 재실행)
COPY package.json package-lock.json* ./
RUN npm ci

# 소스 코드 복사 (develop.watch가 이후 변경분을 컨테이너에 동기화)
# web/.dockerignore 에 의해 node_modules, .next, .env* 제외됨
COPY . .

EXPOSE 3000

CMD ["npm", "run", "dev"]
